name: attendance_app
description: Geo-fence WiFi Attendance System
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Core dependencies (ALL FREE)
  geolocator: ^10.1.0
  permission_handler: ^11.0.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  connectivity_plus: ^5.0.2
  network_info_plus: ^4.1.0
  device_info_plus: ^9.1.0
  flutter_secure_storage: ^9.0.0
  intl: ^0.18.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  
  # UI Dependencies
  google_fonts: ^6.1.0
  flutter_spinkit: ^5.2.0
  fluttertoast: ^8.2.4
  curved_navigation_bar: ^1.0.3

  feather_icons: ^1.2.0
  flutter_phosphor_icons: ^0.0.1+6
  iconsax: ^0.0.8
  heroicons: ^0.11.0
  flutter_svg: ^2.2.2
  shimmer: ^3.0.0
  animated_text_kit: ^4.3.0
  lottie: ^3.3.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true